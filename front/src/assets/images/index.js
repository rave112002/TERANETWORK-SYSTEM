import loginBg from "./login-bg.webp";

/**
 * Decorative imagery. `loginBg` is the backdrop of the auth pages' brand panel —
 * a dark abstract graphic, which is why white type sits on it in both themes.
 */
export { loginBg };
